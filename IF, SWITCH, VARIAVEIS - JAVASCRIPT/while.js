//A declaração while cria um laço que executa uma repetição especifica enquanto a condição de teste for avaliada como verdadeira.
// A condição é avaliada antes da execução da rotina.
var x = 0;

while(x <= 5){
    console.log('Testando repetiçã ' + x);
    x++ //incremento ++ // decremento --
}
var arr = ['teste', 'testando', 'a', 'b']
var y = 0;

while(y <=3){
    console.log(arr[y]);
    y++
}